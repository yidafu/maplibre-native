/**
 * AnnotationType - 标注类型枚举
 * 
 * 定义地图上支持的标注类型
 */
export enum AnnotationType {
  /**
   * 点标注（Marker）
   */
  MARKER = 'marker',
  
  /**
   * 折线标注（Polyline）
   */
  POLYLINE = 'polyline',
  
  /**
   * 多边形标注（Polygon）
   */
  POLYGON = 'polygon'
}

