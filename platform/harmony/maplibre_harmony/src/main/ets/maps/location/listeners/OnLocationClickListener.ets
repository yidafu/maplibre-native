/**
 * Listener for location component clicks.
 */
export interface OnLocationClickListener {
  /**
   * Invoked when the user taps the location indicator.
   */
  onLocationComponentClick(): void;
}

